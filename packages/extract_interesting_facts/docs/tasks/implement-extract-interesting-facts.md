---
title: "Implement extract_interesting_facts Module"
task_type: "feature"
affected_package: "packages/extract_interesting_facts"
spec: "packages/extract_interesting_facts/docs/module-spec.md"
---

# Task: Implement `extract_interesting_facts` Module

## Goal

Set up the complete package scaffold from scratch and implement the `extract_interesting_facts` module per its spec. The module generates Dutch-to-Russian linguistic explanations (interesting facts) for caller-provided Dutch text. It must follow all patterns established by the `translate_text_bidirectional` reference module and satisfy spec requirements FR-1 through FR-16 and acceptance criteria AC-1 through AC-9.

---

## Context and References

### Spec to implement
- **`packages/extract_interesting_facts/docs/module-spec.md`** — the authoritative contract for all public interfaces, failure modes, and acceptance criteria. Read in full before starting.

### Reference module to follow for all patterns
- **`packages/translate_text_bidirectional/`** — all scaffold files, service architecture, test structure, and code style must mirror this module. Specific files:
  - `Makefile` — identical target structure (`lint`, `test-unit`, `test-e2e`, `check`)
  - `pyproject.toml` — build config, dependency list, dev group
  - `ruff.toml` — linter configuration (copy verbatim)
  - `.jscpd.json` — copy-detection config (copy verbatim)
  - `vulture_whitelist.py` — dead-code whitelist pattern
  - `pytest.ini` — pytest configuration (copy verbatim, update asyncio filter warning class name if needed)
  - `src/nl_processing/translate_text_bidirectional/service.py` — service class architecture, chain construction pattern, APIError wrapping, constructor signature
  - `src/nl_processing/translate_text_bidirectional/prompts/generate_nl_ru_bidirectional_prompt.py` — prompt generation script pattern using `save_prompt`
  - `tests/conftest.py` — `AsyncChainMock`, `AsyncChainMockError` shared helpers
  - `tests/unit/translate_text_bidirectional/conftest.py` — unit-test conftest wiring helpers
  - `tests/unit/translate_text_bidirectional/test_bidirectional_translator.py` — unit test style and monkeypatch usage
  - `tests/unit/translate_text_bidirectional/test_error_handling.py` — error-wrapping test pattern
  - `tests/e2e/translate_text_bidirectional/assertions.py` — chatter assertion helper
  - `tests/e2e/translate_text_bidirectional/test_nl_to_ru_quality.py` — e2e test pattern (one consolidated call, multiple assertions)

### Shared core types (do not redefine)
- **`packages/core/src/nl_processing/core/models.py`** — `Language` enum (`Language.NL = "nl"`, `Language.RU = "ru"`)
- **`packages/core/src/nl_processing/core/exceptions.py`** — `APIError`
- **`packages/core/src/nl_processing/core/prompts.py`** — `build_llm_kwargs(model, service_tier, reasoning_effort, temperature)`, `load_prompt(path: str) -> ChatPromptTemplate`
- **`packages/core/src/nl_processing/core/scripts/prompt_author.py`** — `save_prompt(prompt, output_path)` used in the `__main__` block of every prompt-generation script

---

## Key Design Decisions (already resolved — do not re-evaluate)

| Decision | Resolution |
|---|---|
| Source/target language pair | Only `Language.NL` → `Language.RU` in V1 |
| Blank input behavior | `raise ValueError` (NOT return empty string like `translate_text`) |
| Tool-calling | **None** — plain text response, no `bind_tools` |
| LLM invocation pattern | `prompt | ChatOpenAI(**llm_kwargs)` → `response.content` |
| Mode switching | Prompt-driven: the system prompt + few-shot examples teach the model to self-select lexical-item vs. sentence mode |
| Mandatory-fact validation | Post-response heuristic content checks raise `APIError` on missing required facts |
| Constructor defaults | `model="gpt-4.1-mini"`, `service_tier="priority"` (identical to `translate_text`) |
| Supported-pair check | `frozenset` pattern identical to reference; unsupported → `ValueError` during construction |
| Chain invocation input key | `{"text": [HumanMessage(content=text)]}` — identical to reference |

---

## Files to Create

### Package scaffold

| File | Action |
|---|---|
| `pyproject.toml` | Create — see spec below |
| `Makefile` | Create — identical structure to reference |
| `ruff.toml` | Create — copy verbatim from reference |
| `.jscpd.json` | Create — copy verbatim from reference |
| `vulture_whitelist.py` | Create — adapted for this module |
| `pytest.ini` | Create — copy from reference, update warning filter class if necessary |
| `uv.lock` | Generated by `uv sync` — run after `pyproject.toml` is ready |

### Source package

| File | Action |
|---|---|
| `src/nl_processing/extract_interesting_facts/__init__.py` | Create empty (namespace package) |
| `src/nl_processing/extract_interesting_facts/service.py` | Create — core implementation |
| `src/nl_processing/extract_interesting_facts/prompts/__init__.py` | Create empty |
| `src/nl_processing/extract_interesting_facts/prompts/generate_nl_ru_facts_prompt.py` | Create — prompt generation script |
| `src/nl_processing/extract_interesting_facts/prompts/nl_ru_facts.json` | Generated artifact — produced by running the generation script |

### Tests

| File | Action |
|---|---|
| `tests/__init__.py` | Create empty |
| `tests/conftest.py` | Create — `AsyncChainMock`, `AsyncChainMockError`, `make_plain_text_response` helpers (adapted — no tool_calls, just `.content`) |
| `tests/unit/__init__.py` | Create empty |
| `tests/unit/extract_interesting_facts/__init__.py` | Create empty |
| `tests/unit/extract_interesting_facts/conftest.py` | Create — wires helpers from `tests/conftest.py` |
| `tests/unit/extract_interesting_facts/test_extractor.py` | Create — constructor and happy-path unit tests |
| `tests/unit/extract_interesting_facts/test_error_handling.py` | Create — error-wrapping and blank-input unit tests |
| `tests/e2e/__init__.py` | Create empty |
| `tests/e2e/extract_interesting_facts/__init__.py` | Create empty |
| `tests/e2e/extract_interesting_facts/assertions.py` | Create — chatter assertion + fact-presence helpers |
| `tests/e2e/extract_interesting_facts/test_nl_ru_quality.py` | Create — consolidated e2e tests per AC |

---

## `pyproject.toml` Specification

```toml
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nl-processing-extract-interesting-facts"
version = "1.0.0"
description = "Dutch linguistic facts extraction package for nl_processing"
requires-python = ">=3.12"
dependencies = [
  "nl-processing-core",
  "langchain-core>=0.3,<1",
  "langchain-openai>=0.3,<1",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["nl_processing.extract_interesting_facts*"]
namespaces = true

[tool.setuptools.package-data]
"nl_processing.extract_interesting_facts.prompts" = ["*.json"]

[dependency-groups]
dev = [
  "pytest>=9.0.2,<10",
  "pytest-asyncio>=1.3.0,<2",
  "pytest-xdist>=3.8.0,<4",
  "ruff>=0.15.0,<0.16",
  "pylint>=4.0.4,<5",
  "vulture>=2.14.0,<3",
]

[tool.uv.sources]
nl-processing-core = { path = "../core" }
```

Note: `pydantic` is **not** a direct dependency (no `BaseModel` tool schemas needed — plain text response).

---

## `Makefile` Specification

```makefile
lint:
	uv sync --group dev
	uv run ruff format src tests
	uv run ruff check --fix src tests
	uv run pylint src tests --load-plugins=pylint.extensions.bad_builtin --disable=all --enable=C0302,W0141 --max-module-lines=200 --bad-functions=hasattr,getattr,setattr
	uv run vulture src tests vulture_whitelist.py
	@output=$$(npx jscpd --config .jscpd.json --exitCode 1 src tests 2>&1); rc=$$?; [ $$rc -eq 0 ] || { echo "$$output"; exit $$rc; }

test-unit:
	uv sync --group dev
	uv run pytest -n auto tests/unit

test-e2e:
	uv sync --group dev
	doppler run -- uv run pytest -n auto tests/e2e

check: lint test-unit test-e2e
```

---

## Service Implementation (`service.py`)

### Architecture

The service is simpler than `translate_text_bidirectional` because it uses **plain text output** (no tool-calling). The chain is `prompt | ChatOpenAI(**llm_kwargs)` and the response text is read from `response.content`.

```python
# Key imports
import pathlib
from langchain_core.messages import HumanMessage
from langchain_core.runnables import Runnable
from langchain_openai import ChatOpenAI
from nl_processing.core.exceptions import APIError
from nl_processing.core.models import Language
from nl_processing.core.prompts import build_llm_kwargs, load_prompt

_PROMPTS_DIR = pathlib.Path(__file__).parent / "prompts"
_PROMPT_FILE = "nl_ru_facts.json"
_SUPPORTED_PAIRS: set[frozenset[str]] = {frozenset({"nl", "ru"})}
```

### Constructor logic

```python
class InterestingFactsExtractor:
    def __init__(
        self,
        *,
        source_language: Language,
        target_language: Language,
        model: str = "gpt-4.1-mini",
        service_tier: str | None = "priority",
    ) -> None:
        pair = frozenset({source_language.value, target_language.value})
        if pair not in _SUPPORTED_PAIRS:
            supported = ", ".join(
                "->".join(sorted(item)) for item in sorted(_SUPPORTED_PAIRS, key=lambda item: tuple(sorted(item)))
            )
            msg = f"Unsupported language pair {source_language.value}->{target_language.value}. Supported: {supported}"
            raise ValueError(msg)

        self._source_language = source_language
        self._target_language = target_language
        prompt = load_prompt(str(_PROMPTS_DIR / _PROMPT_FILE))
        llm_kwargs = build_llm_kwargs(
            model=model,
            service_tier=service_tier,
            reasoning_effort=None,
            temperature=None,
        )
        llm = ChatOpenAI(**llm_kwargs)
        self._chain: Runnable = prompt | llm
```

Key difference from reference: `ChatOpenAI(**llm_kwargs)` — no `.bind_tools(...)`.

### `extract` method

```python
    async def extract(self, text: str) -> str:
        if not text.strip():
            raise ValueError("Input text must not be blank or whitespace-only")

        try:
            response = await self._chain.ainvoke({"text": [HumanMessage(content=text)]})
            result_text: str = response.content
        except Exception as e:
            raise APIError(str(e)) from e

        _validate_response(result_text, text)
        return result_text
```

### `_validate_response` internal function

This function validates mandatory content requirements (FM-4, FR-14). It raises `APIError` when a required fact is missing. It is a module-level private function (not a method), so it is not part of the public contract.

The validation is deliberately **lenient** to avoid false positives from legitimate linguistic output, while catching clearly empty or useless responses:

1. If `result_text` is empty or whitespace-only → `raise APIError("Model returned empty response")`
2. No other hard-coded keyword checks at this layer — the prompt's few-shot examples are the primary quality enforcement mechanism. 

**Rationale:** Mandatory-fact validation via keyword sniffing (e.g., checking for "de" or "het" in Russian prose) is brittle and language-specific. The prompt guarantees these via few-shot examples. AC-2, AC-3, AC-4 are verified in e2e tests by actually calling the model, not by unit-testing internal keyword guards. This keeps the module lean and avoids false positives when the model legitimately explains a verb or idiom without needing to spell "de" or "het".

---

## Prompt Design (`generate_nl_ru_facts_prompt.py`)

### Script structure

Follows `generate_nl_ru_bidirectional_prompt.py` exactly:
- Defines `SYSTEM_INSTRUCTION` string
- Defines `EXAMPLE_N_INPUT` / `EXAMPLE_N_OUTPUT` constants
- Defines `build_prompt() -> ChatPromptTemplate`
- Uses `MessagesPlaceholder(variable_name="text")`
- `__main__` block calls `save_prompt(build_prompt(), str(OUTPUT_PATH))`
- `OUTPUT_PATH = Path(__file__).parent / "nl_ru_facts.json"`

### System instruction (in Russian)

```text
Вы — лингвистический эксперт по нидерландскому языку. 
Ваша задача — объяснять нидерландский текст на русском языке, давая интересные 
лингвистические факты о словах, грамматике и составе.

Для одиночного слова или короткого выражения (лексемы):
- Укажите часть речи.
- Для существительных — укажите артикль (de или het) и объясните почему, если это интересно.
- Для глаголов — дайте полное спряжение в настоящем времени (ik, jij, hij/zij/het, wij, jullie, zij), 
  укажите, правильный или неправильный глагол.
- Для составных слов — разберите состав и объясните части.
- Добавьте минимум один пример использования или интересный факт об употреблении.

Для предложения:
- Разберите по словам: дайте краткий перевод или глоссу для каждого значимого слова.
- Объясните строение предложения: подлежащее, сказуемое, дополнение (если применимо).
- Укажите время и грамматически интересные конструкции.
- Если предложение является устойчивым выражением, идиомой или фразеологизмом — обязательно скажите об этом.

Правила оформления ответа:
- Пишите только по-русски (нидерландские формы — только как данные анализа).
- Не используйте приветствия, извинения и вводные фразы («Конечно!», «Отлично!» и т.п.).
- Не добавляйте заключительные фразы («Надеюсь, это помогло» и т.п.).
- Отвечайте сразу по существу.
```

### Few-shot examples

Cover all mandatory input types per the spec. Use `HumanMessage` for input and `AIMessage` for output (no `ToolMessage` — plain text, no tool calls).

**Example 1 — Dutch noun with `de` article**

```
INPUT: "hond"
OUTPUT:
«Hond» — существительное мужского рода.
🔑 Артикль: de hond (общий род).

Значение: собака.

Во множественном числе: de honden.

Пример использования:
• De hond loopt in het park. — Собака гуляет в парке.
```

**Example 2 — Dutch noun with `het` article**

```
INPUT: "huis"
OUTPUT:
«Huis» — существительное среднего рода.
🔑 Артикль: het huis (средний род).

Значение: дом.

Во множественном числе: de huizen (обратите внимание на нестандартное множественное число с -en и изменением написания).

Пример использования:
• Het huis staat aan de rivier. — Дом стоит у реки.
```

**Example 3 — Dutch regular verb with full conjugation**

```
INPUT: "werken"
OUTPUT:
«Werken» — глагол, инфинитив. Правильный глагол.

Значение: работать.

Спряжение в настоящем времени:
• ik werk — я работаю
• jij werkt — ты работаешь
• hij/zij/het werkt — он/она/оно работает
• wij werken — мы работаем
• jullie werken — вы работаете (второе лицо множественного числа)
• zij werken — они работают

Пример использования:
• Zij werken elke dag hard. — Они каждый день усердно работают.
```

**Example 4 — Dutch irregular verb**

```
INPUT: "zijn"
OUTPUT:
«Zijn» — глагол, инфинитив. Неправильный глагол (один из наиболее употребительных).

Значение: быть.

Спряжение в настоящем времени:
• ik ben — я есть/являюсь
• jij bent — ты есть/являешься
• hij/zij/het is — он/она/оно есть
• wij zijn — мы есть/являемся
• jullie zijn — вы есть/являетесь (второе лицо множественного числа)
• zij zijn — они есть/являются

Пример использования:
• Jullie zijn goede vrienden. — Вы хорошие друзья.
```

**Example 5 — Dutch compound word**

```
INPUT: "ziekenhuis"
OUTPUT:
«Ziekenhuis» — существительное, составное слово.
🔑 Артикль: het ziekenhuis (средний род).

Состав: ziek (больной/sick) + en (соединительный элемент) + huis (дом/house).
Буквально: «дом для больных» → больница.

Во множественном числе: de ziekenhuizen.

Пример использования:
• Het ziekenhuis is vlak bij. — Больница совсем рядом.
```

**Example 6 — Dutch sentence (sentence mode)**

```
INPUT: "Ik ga morgen naar de markt."
OUTPUT:
Разбор предложения: «Ik ga morgen naar de markt.»

Пословный глоссарий:
• ik — я (подлежащее)
• ga — иду/еду (гл. gaan, неправильный; сказуемое)
• morgen — завтра (обстоятельство времени)
• naar — в/на (предлог направления)
• de markt — рынок (дополнение с артиклем de)

Структура предложения:
Порядок слов: Subj – V – Adv.времени – Prep.группа (стандартный нидерландский порядок).
Время: настоящее с будущим значением (ga + контекст morgen).
```

**Example 7 — Dutch idiom/fixed expression**

```
INPUT: "dat klopt"
OUTPUT:
«Dat klopt» — устойчивое выражение (фразеологизм разговорного регистра).

Дословно: dat — это/то, klopt — стучит (гл. kloppen — стучать).
Идиоматическое значение: «это верно», «правильно», «точно».

Употребление:
Используется как разговорный аналог «да, именно так» в качестве подтверждения. 
Например: — Is dit jouw fiets? — Dat klopt! (— Это твой велосипед? — Да, верно!)
```

### `build_prompt()` structure

```python
from langchain_core.messages import AIMessage, HumanMessage, SystemMessage
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder

def build_prompt() -> ChatPromptTemplate:
    return ChatPromptTemplate.from_messages([
        SystemMessage(content=SYSTEM_INSTRUCTION),
        # Example 1: noun with 'de'
        HumanMessage(content=EXAMPLE_1_INPUT),
        AIMessage(content=EXAMPLE_1_OUTPUT),
        # Example 2: noun with 'het'
        HumanMessage(content=EXAMPLE_2_INPUT),
        AIMessage(content=EXAMPLE_2_OUTPUT),
        # Example 3: regular verb with conjugation
        HumanMessage(content=EXAMPLE_3_INPUT),
        AIMessage(content=EXAMPLE_3_OUTPUT),
        # Example 4: irregular verb
        HumanMessage(content=EXAMPLE_4_INPUT),
        AIMessage(content=EXAMPLE_4_OUTPUT),
        # Example 5: compound word
        HumanMessage(content=EXAMPLE_5_INPUT),
        AIMessage(content=EXAMPLE_5_OUTPUT),
        # Example 6: sentence mode
        HumanMessage(content=EXAMPLE_6_INPUT),
        AIMessage(content=EXAMPLE_6_OUTPUT),
        # Example 7: idiom/fixed expression
        HumanMessage(content=EXAMPLE_7_INPUT),
        AIMessage(content=EXAMPLE_7_OUTPUT),
        # Actual input
        MessagesPlaceholder(variable_name="text"),
    ])
```

Note: **No `ToolMessage`** — this module does not use tool-calling, so the few-shot pair is `HumanMessage` + `AIMessage` only, which differs from the reference module's triple pattern.

### Generating the JSON artifact

After writing the script, run:

```bash
uv sync --group dev
uv run python src/nl_processing/extract_interesting_facts/prompts/generate_nl_ru_facts_prompt.py
```

This produces `nl_ru_facts.json`. The JSON file is committed alongside the script. The script is the source of truth.

---

## `vulture_whitelist.py` Specification

```python
from nl_processing.extract_interesting_facts.service import InterestingFactsExtractor

InterestingFactsExtractor.extract  # type: ignore[misc]

_source_language  # noqa: F821
_target_language  # noqa: F821

__all__ = ["InterestingFactsExtractor"]
```

---

## Unit Tests

### `tests/conftest.py` — Shared helpers

The reference module's `conftest.py` uses `make_tool_response` which builds a `SimpleNamespace` with `tool_calls`. This module returns **plain text** via `response.content`, so the helpers differ:

```python
"""Shared test helpers for async chain mocking."""

from types import SimpleNamespace


class AsyncChainMock:
    """Async mock for a LangChain chain returning plain-text content."""

    def __init__(self, return_value: SimpleNamespace) -> None:
        self.ainvoke_calls: list[dict[str, list[object]]] = []
        self._return_value = return_value

    async def ainvoke(self, input_dict: dict[str, list[object]]) -> SimpleNamespace:
        self.ainvoke_calls.append(input_dict)
        return self._return_value


class AsyncChainMockError:
    """Async mock that raises on ainvoke."""

    def __init__(self, exception: Exception) -> None:
        self._exception = exception

    async def ainvoke(self, _input_dict: dict[str, list[object]]) -> SimpleNamespace:
        raise self._exception


def make_text_response(content: str) -> SimpleNamespace:
    """Build a fake LLM response with plain .content (no tool_calls)."""
    resp = SimpleNamespace()
    resp.content = content
    return resp
```

### `tests/unit/extract_interesting_facts/conftest.py`

```python
from tests.conftest import (
    AsyncChainMock as _AsyncChainMock,
    AsyncChainMockError as _AsyncChainMockError,
    make_text_response as _make_text_response,
)

__all__ = [
    "_AsyncChainMock",
    "_AsyncChainMockError",
    "_make_text_response",
]
```

### `tests/unit/extract_interesting_facts/test_extractor.py`

Tests to implement (map to spec requirements):

| Test | What it validates | Spec ref |
|---|---|---|
| `test_constructor_valid_pair_nl_ru` | NL→RU constructor succeeds, stores `_source_language`, `_target_language`, `_chain` | FR-1, FR-3 |
| `test_constructor_unsupported_pair_nl_nl` | Same language raises `ValueError` with "Unsupported language pair" | FR-3, FM-1 |
| `test_constructor_unsupported_pair_ru_ru` | Same | FR-3, FM-1 |
| `test_constructor_custom_model` | `model` and `service_tier` overrides accepted | FR-1 |
| `test_constructor_default_model_and_tier` | Default `model="gpt-4.1-mini"`, `service_tier="priority"` | IF-1 |
| `test_extract_happy_path` | Valid Dutch text returns mocked response content | FR-2 |
| `test_extract_invokes_chain_with_correct_structure` | `ainvoke` called once with `{"text": [HumanMessage(...)]}` | FR-2 |

### `tests/unit/extract_interesting_facts/test_error_handling.py`

| Test | What it validates | Spec ref |
|---|---|---|
| `test_blank_input_raises_value_error` | `""` raises `ValueError` | FR-4, FM-2 |
| `test_whitespace_input_raises_value_error` | `"   \n\t  "` raises `ValueError` | FR-4, FM-2 |
| `test_chain_exception_raises_api_error` | `RuntimeError` from chain is wrapped as `APIError` | FR-15, FM-3 |
| `test_api_error_preserves_cause` | `__cause__` is the original exception | FR-15, FM-3 |
| `test_api_error_various_exceptions` | `ValueError`, `ConnectionError`, `KeyError` all become `APIError` | FR-15 |

All unit tests set `monkeypatch.setenv("OPENAI_API_KEY", "test-key")` before constructing the extractor, identical to reference.

---

## E2E Tests

### `tests/e2e/extract_interesting_facts/assertions.py`

```python
LLM_CHATTER_PREFIXES = [
    "Конечно!",
    "Отлично!",
    "Sure,",
    "Of course",
    "Here is",
    "Certainly",
    "Надеюсь, это",
    "Рад помочь",
]


def assert_no_llm_chatter(result: str) -> None:
    for prefix in LLM_CHATTER_PREFIXES:
        assert not result.startswith(prefix), f"Output starts with LLM chatter: '{prefix}'"


def assert_contains_any(result: str, candidates: list[str], label: str) -> None:
    """Assert that result contains at least one of the candidate strings."""
    found = any(c in result for c in candidates)
    assert found, f"Expected {label} in result. Got:\n{result}"
```

### `tests/e2e/extract_interesting_facts/test_nl_ru_quality.py`

Design principle: **one real API call per test function, validating multiple related assertions** (mirrors `test_nl_to_ru_feature_matrix_quality` pattern). This minimizes API cost while covering all ACs.

| Test function | Input | Assertions | AC | Spec |
|---|---|---|---|---|
| `test_noun_de_article_and_pos` | `"hond"` | Result non-empty; no chatter; contains "de" (article check); contains any of \["существительное", "noun", "Существительное"\] (POS); contains any of \["пример", "Пример", "употребл"\] (usage note) | AC-2 | FR-5, FR-6, FR-9 |
| `test_noun_het_article` | `"huis"` | Result non-empty; no chatter; contains "het" (article check) | AC-2 | FR-6 |
| `test_verb_conjugation_and_regularity` | `"werken"` | Result non-empty; no chatter; contains "jullie" (2nd person plural check); contains any of \["правильный", "правильн", "regelmatig"\] (regularity statement) | AC-3 | FR-7 |
| `test_compound_word_decomposition` | `"ziekenhuis"` | Result non-empty; no chatter; contains any of \["ziek", "huis", "состав", "части"\] (composition mention) | AC-4 | FR-8 |
| `test_sentence_mode_lighter_per_token` | `"Ik ga morgen naar de markt."` | Result non-empty; no chatter; contains any of \["ik", "ga", "morgen"\] (token glosses); result length < 1500 chars (lighter than lexical mode — heuristic guard against full per-token dossiers) | AC-5 | FR-10 |
| `test_idiom_identified` | `"dat klopt"` | Result non-empty; no chatter; contains any of \["идиом", "выражение", "фразеологизм", "устойчив"\] (idiom identification) | AC-6 | FR-11 |
| `test_multi_sentence_not_rejected` | `"De hond loopt in het park. Het is een mooie dag."` | Does NOT raise; result non-empty | AC-9 | FR-16 |

No `time.perf_counter` latency assertions (unlike reference) — latency is not in this module's NFRs.

All e2e tests are `@pytest.mark.asyncio` and construct a fresh `InterestingFactsExtractor(source_language=Language.NL, target_language=Language.RU)` without monkeypatching (real Doppler-injected `OPENAI_API_KEY`).

---

## Implementation Steps

### Step 1 — Create the package scaffold

1. Create `pyproject.toml` per spec above.
2. Create `Makefile` per spec above.
3. Copy `ruff.toml` verbatim from `packages/translate_text_bidirectional/ruff.toml`.
4. Copy `.jscpd.json` verbatim from `packages/translate_text_bidirectional/.jscpd.json`.
5. Copy `pytest.ini` verbatim from `packages/translate_text_bidirectional/pytest.ini`.
6. Create `vulture_whitelist.py` per spec above.
7. Run `uv sync --group dev` to generate `uv.lock`.

### Step 2 — Create source package skeleton

1. Create `src/nl_processing/extract_interesting_facts/__init__.py` (empty).
2. Create `src/nl_processing/extract_interesting_facts/prompts/__init__.py` (empty).

### Step 3 — Write and run the prompt generation script

1. Create `src/nl_processing/extract_interesting_facts/prompts/generate_nl_ru_facts_prompt.py` with all 7 few-shot examples per spec above.
2. Run: `uv run python src/nl_processing/extract_interesting_facts/prompts/generate_nl_ru_facts_prompt.py`
3. Verify `nl_ru_facts.json` is created in the same directory.

### Step 4 — Implement `service.py`

Implement `InterestingFactsExtractor` and the private `_validate_response` helper per the design above.

### Step 5 — Create all test files

1. `tests/__init__.py` (empty)
2. `tests/conftest.py` — plain-text mock helpers
3. `tests/unit/__init__.py` (empty)
4. `tests/unit/extract_interesting_facts/__init__.py` (empty)
5. `tests/unit/extract_interesting_facts/conftest.py`
6. `tests/unit/extract_interesting_facts/test_extractor.py`
7. `tests/unit/extract_interesting_facts/test_error_handling.py`
8. `tests/e2e/__init__.py` (empty)
9. `tests/e2e/extract_interesting_facts/__init__.py` (empty)
10. `tests/e2e/extract_interesting_facts/assertions.py`
11. `tests/e2e/extract_interesting_facts/test_nl_ru_quality.py`

### Step 6 — Replace the stub `Makefile`

The package currently has a stub `Makefile` that only checks for `docs/module-spec.md`. Replace it entirely with the full Makefile from Step 1.

### Step 7 — Quality gate

Run `make check` and ensure it exits 0. Fix any linter or test failures.

---

## Acceptance Criteria

All the following must be satisfied before the task is complete:

### AC-1 (spec AC-1, VAL-8, FR-3)
- `InterestingFactsExtractor(Language.NL, Language.RU)` succeeds.
- `InterestingFactsExtractor(Language.RU, Language.RU)` raises `ValueError` during construction.
- `InterestingFactsExtractor(Language.NL, Language.NL)` raises `ValueError` during construction.
- **Test:** `test_constructor_valid_pair_nl_ru`, `test_constructor_unsupported_pair_nl_nl`, `test_constructor_unsupported_pair_ru_ru` in unit tests.

### AC-2 (spec AC-2, VAL-2, FR-5, FR-6, FR-9)
- Dutch noun `"hond"` → response contains `"de"`, indicates part of speech, and includes usage note.
- Dutch noun `"huis"` → response contains `"het"`.
- **Test:** `test_noun_de_article_and_pos`, `test_noun_het_article` in e2e tests.

### AC-3 (spec AC-3, VAL-3, FR-7)
- Dutch verb `"werken"` → response contains `"jullie"` (second-person plural form) and mentions regularity.
- **Test:** `test_verb_conjugation_and_regularity` in e2e tests.

### AC-4 (spec AC-4, VAL-4, FR-8)
- Dutch compound `"ziekenhuis"` → response identifies meaningful parts (`"ziek"` and `"huis"` or describes composition in Russian).
- **Test:** `test_compound_word_decomposition` in e2e tests.

### AC-5 (spec AC-5, VAL-5, FR-10)
- Sentence `"Ik ga morgen naar de markt."` → response includes token-level glosses without a full per-token lexical dossier (response length heuristic + token keyword presence).
- **Test:** `test_sentence_mode_lighter_per_token` in e2e tests.

### AC-6 (spec AC-6, VAL-5, FR-11)
- Idiom `"dat klopt"` → response identifies it as an expression/idiom/fixed phrase.
- **Test:** `test_idiom_identified` in e2e tests.

### AC-7 (spec AC-7, VAL-8, FR-4)
- `await extractor.extract("")` raises `ValueError`.
- `await extractor.extract("   \n  ")` raises `ValueError`.
- **Test:** `test_blank_input_raises_value_error`, `test_whitespace_input_raises_value_error` in unit tests.

### AC-8 (spec AC-8, VAL-7, FR-15)
- Chain exception → `APIError` with original exception as `__cause__`.
- **Test:** `test_chain_exception_raises_api_error`, `test_api_error_preserves_cause` in unit tests.

### AC-9 (spec AC-9, VAL-9, FR-16)
- Multi-sentence input does not raise a blanket rejection error.
- **Test:** `test_multi_sentence_not_rejected` in e2e tests.

### Quality gates (non-negotiable)
- `make lint` passes (ruff format + ruff check + pylint + vulture + jscpd all green).
- `make test-unit` passes (all unit tests green).
- `make test-e2e` passes (all e2e tests green — requires `doppler run`).
- `make check` exits 0.
- No file exceeds 180 lines (soft limit) / 200 lines (hard limit enforced by pylint).
- Code follows the repo's zero-fallback, fail-fast style: no `dict.get`, no `os.getenv`, no `Any`.

---

## LangChain API Reference

All LangChain usage must follow the patterns already established in this repo. Key functions and classes:

### `ChatPromptTemplate.from_messages` with `MessagesPlaceholder`
Source: `packages/translate_text_bidirectional/src/nl_processing/translate_text_bidirectional/prompts/generate_nl_ru_bidirectional_prompt.py`

```python
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
from langchain_core.messages import SystemMessage, HumanMessage, AIMessage

prompt = ChatPromptTemplate.from_messages([
    SystemMessage(content="...system..."),
    HumanMessage(content="example input"),
    AIMessage(content="example output"),        # plain text — NO ToolMessage
    MessagesPlaceholder(variable_name="text"),  # runtime input slot
])
```

### `save_prompt` / `dumpd`
Source: `packages/core/src/nl_processing/core/scripts/prompt_author.py`

```python
from nl_processing.core.scripts.prompt_author import save_prompt

if __name__ == "__main__":
    save_prompt(build_prompt(), str(OUTPUT_PATH))
```

### `load_prompt`
Source: `packages/core/src/nl_processing/core/prompts.py`

```python
from nl_processing.core.prompts import load_prompt

prompt = load_prompt("/path/to/nl_ru_facts.json")  # returns ChatPromptTemplate
```

### `build_llm_kwargs`
Source: `packages/core/src/nl_processing/core/prompts.py`

```python
from nl_processing.core.prompts import build_llm_kwargs

llm_kwargs = build_llm_kwargs(
    model="gpt-4.1-mini",
    service_tier="priority",
    reasoning_effort=None,
    temperature=None,
)
```

### `ChatOpenAI` chain invocation (plain text, no tools)
Source: `packages/translate_text_bidirectional/src/nl_processing/translate_text_bidirectional/service.py` (adapted — remove `.bind_tools`)

```python
from langchain_openai import ChatOpenAI
from langchain_core.messages import HumanMessage

llm = ChatOpenAI(**llm_kwargs)           # NO .bind_tools(...)
chain = prompt | llm
response = await chain.ainvoke({"text": [HumanMessage(content=text)]})
result_text: str = response.content      # plain text, NOT response.tool_calls[...]
```

---

## Docs Update

After implementation, update `packages/extract_interesting_facts/docs/module-spec.md` **only if** you find a discrepancy between the spec and the implemented behavior that requires a doc fix. Do **not** change the spec to explain implementation details.

No other modules need documentation changes for this task (the cross-module change table in the spec lists "N/A").
